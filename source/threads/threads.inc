    IF !DEF(STRUCT_THREADS_INC)
    DEF STRUCT_THREADS_INC EQU 1

RSRESET

; ROMX bank.  
; If this is 0, the thread is considered unallocated.  
; Written to `rROMB0` if `CONFIG_BANKABLE_ROMX` is 1.
DEF THREAD_BANK_ROMX RB 1

; SRAM bank.
; Written to `rRAMB` if `CONFIG_BANKABLE_SRAM` is 1.
DEF THREAD_BANK_SRAM RB 1

; CGB WRAMX bank.
; Written to `rSVBK` if `CONFIG_BANKABLE_WRAMX` is 1.
DEF THREAD_BANK_WRAMX RB 1

; CGB VRAM bank.
; Written to `rVBK`, if `CONFIG_BANKABLE_VRAM` is 1.
DEF THREAD_BANK_VRAM RB 1

DEF THREAD_REGISTER_PC RW 1
DEF THREAD_REGISTER_SP RW 1
DEF THREAD_REGISTER_BC RW 1
DEF THREAD_REGISTER_DE RW 1
DEF THREAD_REGISTER_HL RW 1
DEF THREAD_REGISTER_AF RW 1

; A thread struct.
DEF THREAD_T RB 0

    ENDC
